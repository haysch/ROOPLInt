class Foo
    int v

    method set(int x)
        v ^= x

class Bar inherits Foo
    int v

    method set(int x)
        v ^= x

class Program
    int[] z
    int u
    Foo f
    Bar b
    int len

    method main()
        //new Foo f

        //copy Foo f b
        len += 5
        new int[len] z
        z[-2] += 1

        //local int x = 0
        //from x = 0 do
        //    skip
        //loop
        //    z[x] += x + 1
        //    u += z[x] + z[x]
        //    x += 1
        //until x = 3
        //z[x] += z[x-1] + z[x-1]
        //delocal int x = 3